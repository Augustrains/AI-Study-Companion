"""认证模块：账号注册、密码/验证码登录、会话令牌与账号资料维护。

定位说明（重要）：
    这是一个**演示级**认证实现，够用于本项目的联调和演示，但不是生产级方案。
    已经做到的：密码不落明文（PBKDF2-HMAC-SHA256 + 每账号随机盐）、令牌带
    HMAC 签名和过期时间、错误信息不区分「账号不存在」和「密码错误」。
    上线前必须补的：
      1. 用户表换成数据库，当前是单文件 JSON，没有并发写保护；
      2. 验证码走真实邮件/短信通道，当前 dev 模式直接把码返回给前端；
      3. 令牌换成可撤销的服务端会话或带 refresh 的 JWT，当前登出只清前端；
      4. 加登录失败次数限制、验证码发送频率限制。
"""

from .module import AuthModule

__all__ = ["AuthModule"]
